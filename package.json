{
  "name": "provenance-action",
  "version": "1.0.0",
  "description": "GitHub Action to validate PROVENANCE.yml files",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/index.js -o dist --source-map --license licenses.txt"
  },
  "keywords": ["github-action", "provenance", "validation", "ai-agent"],
  "author": "Provenance Protocol",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/github": "^5.1.1",
    "js-yaml": "^4.1.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.0"
  }
}
